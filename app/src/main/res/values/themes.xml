<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.GrammarFloat" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#6200EE</item>
        <item name="colorPrimaryDark">#3700B3</item>
        <item name="colorAccent">#03DAC5</item>
        <item name="colorPrimaryContainer">#EADDFF</item>
        <item name="colorOnPrimaryContainer">#21005D</item>
        <item name="colorSurfaceContainer">#F3EDFA</item>
        <item name="colorSurfaceContainerLow">#FAF8FC</item>
        <item name="android:statusBarColor">?android:attr/colorBackground</item>
        <item name="android:windowLightStatusBar">?attr/isLightTheme</item>
    </style>

    <style name="OverlayAnimation">
        <item name="android:windowEnterAnimation">@anim/fade_scale_in</item>
        <item name="android:windowExitAnimation">@anim/fade_scale_out</item>
    </style>

    <!--
        Theme.ProcessText: fully transparent, zero-chrome trampoline activity.

        windowDisablePreview is intentionally NOT set here. Some host apps (Gmail,
        Messages) check that the launched activity produced a window snapshot before
        clearing their text-selection context. Disabling the preview suppresses that
        signal and makes the launch appear to fail.

        The transparent appearance is achieved via windowIsTranslucent +
        windowBackground=transparent alone, without the problematic window flags.
    -->
    <style name="Theme.ProcessText" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>


</resources>
